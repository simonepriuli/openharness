/**
 * @deprecated Use `pnpm --filter @openharness/db db:orgs` from the repo root.
 */
console.error(
  "This script moved to packages/db. Run: pnpm --filter @openharness/db db:orgs",
);
process.exit(1);
