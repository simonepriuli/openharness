#!/bin/sh
set -e
cd "$(git rev-parse --show-toplevel)"
node scripts/sync-vendor-pi-sha.mjs --stage
if [ -x .git/hooks/pre-commit.openharness-backup ]; then
  exec .git/hooks/pre-commit.openharness-backup "$@"
fi
